@model List<KeyValuePair<bool, string>>
@{
    Layout = "_Layout";
}

@using (Html.BeginForm("Create", "Gym", FormMethod.Post)) {
    <fieldset class="container">
        <legend>Update data</legend>
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" required class="form-control" name="Name" id="name" aria-describedby="nameHelp" value="">
        </div>
        <div class="form-group">
            <label for="address">Address</label>
            <input type="text" required class="form-control" name="Address" id="address" value="">
        </div>
        <div class="form-group">
            @foreach (var elem in Model) {
                <div class="custom-control custom-checkbox">
                    <input type="checkbox" class="custom-control-input" id="@elem.Value" value="@elem.Value" name="MachinesName[]" checked="">
                    <label class="custom-control-label" for="@elem.Value">@elem.Value</label>
                </div>
            }
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
    </fieldset>
}


